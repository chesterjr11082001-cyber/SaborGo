apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: saborgo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        env:
        - name: POSTGRES_USER
          value: "saborgo_user"
        - name: POSTGRES_PASSWORD
          value: "saborgo_password"
        - name: POSTGRES_DB
          value: "saborgo_db"
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
      volumes:
      - name: postgres-storage
        hostPath:
          path: /data/postgres
          type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: saborgo
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432